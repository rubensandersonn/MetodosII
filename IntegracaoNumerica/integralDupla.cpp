double det(vector < vector <double> > mat){

}

vector < vector< double> > createJ(Funcao * F, alpha, betha){
	double dxda =
}

for(){
	for(){
		w[i] * w2[j] * f( x( alpha[i], betha[j] ), y( alpha[i], betha[j]) ) * det ( createJ( alpha[i], betha[j] ) );
	}
}